name: Build Termux Firefox Native Sandbox Port v2.4.1

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: firefox-native-sandbox-${{ github.ref }}
  cancel-in-progress: false

env:
  TERMUX_BUILDER_IMAGE_NAME: termux-firefox-builder:sccache

jobs:
  build:
    name: Firefox native sandbox aarch64
    runs-on: ubuntu-26.04
    timeout-minutes: 355

    steps:
      # ============================================================
      # 1. Checkout current upstream Termux packages
      # ============================================================
      - name: Checkout termux-packages
        uses: actions/checkout@v7
        with:
          repository: termux/termux-packages
          ref: master
          fetch-depth: 1

      # ============================================================
      # 2. Verify exact upstream Firefox recipe/version
      # ============================================================
      - name: Verify upstream Firefox recipe
        shell: bash
        run: |
          set -euxo pipefail

          FIREFOX_DIR="x11-packages/firefox"
          BUILD_SH="$FIREFOX_DIR/build.sh"
          MOZCONFIG="$FIREFOX_DIR/mozconfig.cfg"
          EXPECTED_FIREFOX_VERSION="155.0.1"

          test -d "$FIREFOX_DIR"
          test -f "$BUILD_SH"
          test -f "$MOZCONFIG"

          echo "======================================"
          echo "Upstream termux-packages commit"
          echo "======================================"
          git rev-parse HEAD

          echo
          echo "======================================"
          echo "Firefox package version"
          echo "======================================"

          grep '^TERMUX_PKG_VERSION=' "$BUILD_SH"
          grep '^TERMUX_PKG_REVISION=' "$BUILD_SH" || true

          if ! grep -q \
            "^TERMUX_PKG_VERSION=\"$EXPECTED_FIREFOX_VERSION\"$" \
            "$BUILD_SH"; then

            echo
            echo "ERROR: Unsupported Firefox version."
            echo "Expected: $EXPECTED_FIREFOX_VERSION"
            echo
            echo "Review the native sandbox port before"
            echo "using it with another Firefox release."
            exit 1
          fi

          echo
          echo "Firefox version guard: SUCCESS"

          echo
          echo "======================================"
          echo "Original sandbox configuration"
          echo "======================================"

          grep -nE \
            'sandbox|forkserver|ccache|jemalloc|hardening' \
            "$MOZCONFIG" || true

          grep -q \
            '^ac_add_options --disable-sandbox$' \
            "$MOZCONFIG"

      # ============================================================
      # 3. Enable Firefox native sandbox
      # ============================================================
      - name: Configure native sandbox build
        shell: bash
        run: |
          set -euxo pipefail

          MOZCONFIG="x11-packages/firefox/mozconfig.cfg"

          sed -i \
            's/^ac_add_options --disable-sandbox$/ac_add_options --enable-sandbox/' \
            "$MOZCONFIG"

          grep -q \
            '^ac_add_options --disable-forkserver$' \
            "$MOZCONFIG" || \
            echo \
              'ac_add_options --disable-forkserver' \
              >> "$MOZCONFIG"

          grep -q \
            '^ac_add_options --with-ccache=sccache$' \
            "$MOZCONFIG" || \
            echo \
              'ac_add_options --with-ccache=sccache' \
              >> "$MOZCONFIG"

          grep -q \
            '^ac_add_options --enable-sandbox$' \
            "$MOZCONFIG"

          grep -q \
            '^ac_add_options --disable-forkserver$' \
            "$MOZCONFIG"

          grep -q \
            '^ac_add_options --with-ccache=sccache$' \
            "$MOZCONFIG"

          if grep -q \
            '^ac_add_options --disable-sandbox$' \
            "$MOZCONFIG"; then

            echo "ERROR: --disable-sandbox is still present"
            exit 1
          fi

          echo "======================================"
          echo "Modified mozconfig"
          echo "======================================"

          cat "$MOZCONFIG"

      # ============================================================
      # 4. Patch Firefox package recipe
      #
      # PY2:
      #   - Socket / Utility PAC compatibility
      #   - Common getrlimit(RLIMIT_STACK)
      #
      # PY3:
      #   - Utility own /proc/<pid>/stat read-only
      #   - Utility own /proc/<pid>/maps read-only
      #
      # All run after normal Termux patches and 9999 patch.
      # ============================================================
      - name: Patch Firefox package recipe
        shell: bash
        run: |
          set -euxo pipefail

          python3 <<'PY'
          from pathlib import Path
          import re

          path = Path("x11-packages/firefox/build.sh")
          text = path.read_text()

          # --------------------------------------------------------
          # libandroid-glob dependency
          # --------------------------------------------------------
          m = re.search(
              r'^TERMUX_PKG_DEPENDS="([^"]*)"$',
              text,
              flags=re.MULTILINE,
          )

          if not m:
              raise SystemExit(
                  "TERMUX_PKG_DEPENDS not found"
              )

          deps = [
              x.strip()
              for x in m.group(1).split(",")
          ]

          if "libandroid-glob" not in deps:
              try:
                  pos = (
                      deps.index("libandroid-shmem")
                      + 1
                  )
              except ValueError:
                  pos = len(deps)

              deps.insert(
                  pos,
                  "libandroid-glob",
              )

              new_line = (
                  'TERMUX_PKG_DEPENDS="'
                  + ", ".join(deps)
                  + '"'
              )

              text = (
                  text[:m.start()]
                  + new_line
                  + text[m.end():]
              )

          # --------------------------------------------------------
          # libandroid-glob linker dependency
          # --------------------------------------------------------
          old_ldflags = (
              'LDFLAGS+=" -landroid-shmem '
              '-landroid-spawn -llog"'
          )

          new_ldflags = (
              'LDFLAGS+=" -landroid-shmem '
              '-landroid-spawn '
              '-landroid-glob -llog"'
          )

          if new_ldflags not in text:
              if old_ldflags not in text:
                  raise SystemExit(
                      "Expected Firefox LDFLAGS "
                      "line not found"
                  )

              text = text.replace(
                  old_ldflags,
                  new_ldflags,
                  1,
              )

          marker = (
              "termux_step_pre_configure() {\n"
              "\ttermux_setup_nodejs\n"
          )

          if marker not in text:
              raise SystemExit(
                  "termux_step_pre_configure "
                  "marker not found"
              )

          preflight = r'''termux_step_pre_configure() {
          \techo "======================================"
          \techo "Firefox native sandbox Termux/Bionic preflight v2.4.1"
          \techo "======================================"

          # ========================================================
          # Target macro probe
          # ========================================================
          \tlocal _macro_src
          \tlocal _macro_obj

          \t_macro_src="$TERMUX_PKG_TMPDIR/firefox-termux-macros.c"
          \t_macro_obj="$TERMUX_PKG_TMPDIR/firefox-termux-macros.o"

          \tcat > "$_macro_src" <<'EOF'
          #include <sys/cdefs.h>

          #ifndef __TERMUX__
          #error "__TERMUX__ is not defined"
          #endif

          #ifndef __ANDROID__
          #error "__ANDROID__ is not defined"
          #endif

          int firefox_termux_macro_probe(void) {
              return 0;
          }
          EOF

          \t"$CC" \
          \t\t-c \
          \t\t"$_macro_src" \
          \t\t-o "$_macro_obj"

          \techo
          \techo "Target macro probe: SUCCESS"

          # ========================================================
          # Verify Android/Bionic RLIMIT_STACK
          # ========================================================
          \tlocal _rlimit_src
          \tlocal _rlimit_obj

          \t_rlimit_src="$TERMUX_PKG_TMPDIR/firefox-termux-rlimit.cc"
          \t_rlimit_obj="$TERMUX_PKG_TMPDIR/firefox-termux-rlimit.o"

          \tcat > "$_rlimit_src" <<'EOF'
          #include <sys/resource.h>

          static_assert(
              RLIMIT_STACK == 3,
              "Unexpected Android RLIMIT_STACK value"
          );

          int firefox_termux_rlimit_probe(void) {
              return RLIMIT_STACK;
          }
          EOF

          \t"$CXX" \
          \t\t-c \
          \t\t"$_rlimit_src" \
          \t\t-o "$_rlimit_obj"

          \techo "RLIMIT_STACK target probe: SUCCESS"

          # ========================================================
          # PY2: SandboxFilter.cpp
          # ========================================================
          \tpython3 <<'PY2'
          from pathlib import Path

          path = Path(
              "security/sandbox/linux/"
              "SandboxFilter.cpp"
          )

          text = path.read_text()


          def class_bounds(
              source,
              class_name,
              next_class=None,
          ):
              start = source.find(
                  f"class {class_name}"
              )

              if start < 0:
                  raise SystemExit(
                      f"{class_name}: "
                      "class not found"
                  )

              if next_class:
                  end = source.find(
                      f"class {next_class}",
                      start + 1,
                  )

                  if end < 0:
                      raise SystemExit(
                          f"{class_name}: "
                          f"next class {next_class} "
                          "not found"
                      )
              else:
                  end = len(source)

              return start, end


          # --------------------------------------------------------
          # RLIMIT_STACK declaration
          # --------------------------------------------------------
          if "#include <sys/resource.h>" not in text:
              include_marker = (
                  "#include <sys/prctl.h>\n"
              )

              if include_marker not in text:
                  raise SystemExit(
                      "sys/prctl.h include marker "
                      "not found"
                  )

              text = text.replace(
                  include_marker,
                  include_marker
                  + "#include <sys/resource.h>\n",
                  1,
              )

              print(
                  "sys/resource.h include injected"
              )


          # --------------------------------------------------------
          # Socket / Utility PrctlPolicy
          # --------------------------------------------------------
          def prctl_bounds(
              source,
              class_name,
              next_class=None,
          ):
              start, end = class_bounds(
                  source,
                  class_name,
                  next_class,
              )

              segment = source[start:end]

              marker = (
                  "ResultExpr PrctlPolicy() "
                  "const override {"
              )

              p = segment.find(marker)

              if p < 0:
                  raise SystemExit(
                      f"{class_name}: "
                      "PrctlPolicy not found"
                  )

              q = segment.find(
                  "ResultExpr EvaluateSyscall",
                  p,
              )

              if q < 0:
                  raise SystemExit(
                      f"{class_name}: "
                      "EvaluateSyscall boundary "
                      "not found"
                  )

              return (
                  start,
                  end,
                  p,
                  q,
                  segment[p:q],
              )


          def inject_prctl_rules(
              class_name,
              next_class=None,
          ):
              global text

              start, end, p, q, prctl = \
                  prctl_bounds(
                      text,
                      class_name,
                      next_class,
                  )

              need_dumpable = (
                  "PR_GET_DUMPABLE"
                  not in prctl
              )

              need_pac = (
                  "PR_PAC_RESET_KEYS"
                  not in prctl
              )

              if (
                  not need_dumpable
                  and not need_pac
              ):
                  print(
                      f"{class_name}: "
                      "Termux PAC rules "
                      "already present"
                  )
                  return

              segment = text[start:end]

              switch_pos = segment.find(
                  "return Switch(op)",
                  p,
                  q,
              )

              if switch_pos < 0:
                  raise SystemExit(
                      f"{class_name}: "
                      "return Switch(op) "
                      "not found"
                  )

              newline = segment.find(
                  "\n",
                  switch_pos,
              )

              if newline < 0:
                  raise SystemExit(
                      f"{class_name}: "
                      "malformed Switch(op)"
                  )

              insert_at = (
                  start
                  + newline
                  + 1
              )

              rules = [
                  "#if defined(__TERMUX__)\n"
              ]

              if need_dumpable:
                  rules.append(
                      "        "
                      ".Case(PR_GET_DUMPABLE, "
                      "Allow())\n"
                  )

              if need_pac:
                  rules.extend([
                      "#  if defined(__aarch64__)\n",

                      "        "
                      ".Case(PR_PAC_RESET_KEYS,\n",

                      "              "
                      "If(arg2 == PR_PAC_APIAKEY, "
                      "Allow())\n",

                      "                  "
                      ".Else(InvalidSyscall()))\n",

                      "#  endif\n",
                  ])

              rules.append(
                  "#endif\n"
              )

              text = (
                  text[:insert_at]
                  + "".join(rules)
                  + text[insert_at:]
              )

              print(
                  f"{class_name}: "
                  "Termux PAC rules injected"
              )


          inject_prctl_rules(
              "SocketProcessSandboxPolicy",
              "UtilitySandboxPolicy",
          )

          inject_prctl_rules(
              "UtilitySandboxPolicy",
          )


          # --------------------------------------------------------
          # Common getrlimit(RLIMIT_STACK)
          # --------------------------------------------------------
          def inject_common_getrlimit_stack():
              global text

              common_start, common_end = \
                  class_bounds(
                      text,
                      "SandboxPolicyCommon",
                      "ContentSandboxPolicy",
                  )

              segment = text[
                  common_start:common_end
              ]

              function_marker = (
                  "ResultExpr EvaluateSyscall("
                  "int sysno) const override {"
              )

              function_start = segment.find(
                  function_marker
              )

              if function_start < 0:
                  raise SystemExit(
                      "SandboxPolicyCommon: "
                      "EvaluateSyscall not found"
                  )

              function_text = segment[
                  function_start:
              ]

              if (
                  "resource == RLIMIT_STACK"
                  in function_text
              ):
                  print(
                      "SandboxPolicyCommon: "
                      "RLIMIT_STACK rule "
                      "already present"
                  )
                  return

              time_marker = segment.find(
                  "// Timekeeping",
                  function_start,
              )

              if time_marker < 0:
                  raise SystemExit(
                      "SandboxPolicyCommon: "
                      "Timekeeping marker "
                      "not found"
                  )

              switch_pos = segment.rfind(
                  "switch (sysno) {",
                  function_start,
                  time_marker,
              )

              if switch_pos < 0:
                  raise SystemExit(
                      "SandboxPolicyCommon: "
                      "main switch(sysno) "
                      "not found"
                  )

              newline = segment.find(
                  "\n",
                  switch_pos,
              )

              if newline < 0:
                  raise SystemExit(
                      "SandboxPolicyCommon: "
                      "malformed main switch"
                  )

              insert_at = (
                  common_start
                  + newline
                  + 1
              )

              rule = (
                  "#if defined(__TERMUX__) && "
                  "defined(__NR_getrlimit)\n"
                  "      case __NR_getrlimit: {\n"
                  "        Arg<int> resource(0);\n"
                  "        return If("
                  "resource == RLIMIT_STACK, "
                  "Allow())\n"
                  "            .Else("
                  "SandboxPolicyBase::"
                  "EvaluateSyscall(sysno));\n"
                  "      }\n"
                  "#endif\n"
                  "\n"
              )

              text = (
                  text[:insert_at]
                  + rule
                  + text[insert_at:]
              )

              print(
                  "SandboxPolicyCommon: "
                  "RLIMIT_STACK rule injected"
              )


          inject_common_getrlimit_stack()

          path.write_text(text)
          result = path.read_text()


          # --------------------------------------------------------
          # Verify Socket / Utility PAC
          # --------------------------------------------------------
          def verify_prctl(
              class_name,
              next_class=None,
          ):
              _, _, _, _, prctl = \
                  prctl_bounds(
                      result,
                      class_name,
                      next_class,
                  )

              bad_patterns = (
                  ".CASES(("
                  + "PR_GET_DUMPABLE)",

                  ".CASES(("
                  + "PR_PAC_RESET_KEYS)",
              )

              for bad in bad_patterns:
                  if bad in prctl:
                      raise SystemExit(
                          f"{class_name}: "
                          "invalid injected API: "
                          f"{bad}"
                      )

              required = (
                  ".Case("
                  "PR_GET_DUMPABLE, Allow())",

                  ".Case("
                  "PR_PAC_RESET_KEYS,",

                  "arg2 == PR_PAC_APIAKEY",
              )

              for token in required:
                  if token not in prctl:
                      raise SystemExit(
                          f"{class_name}: "
                          "expected rule missing: "
                          f"{token}"
                      )

              print(
                  f"{class_name}: "
                  "PrctlPolicy syntax sanity "
                  "SUCCESS"
              )


          verify_prctl(
              "SocketProcessSandboxPolicy",
              "UtilitySandboxPolicy",
          )

          verify_prctl(
              "UtilitySandboxPolicy",
          )


          # --------------------------------------------------------
          # Verify Common getrlimit(RLIMIT_STACK)
          # --------------------------------------------------------
          def verify_common_getrlimit():
              common_start, common_end = \
                  class_bounds(
                      result,
                      "SandboxPolicyCommon",
                      "ContentSandboxPolicy",
                  )

              common = result[
                  common_start:common_end
              ]

              if (
                  "#include <sys/resource.h>"
                  not in result
              ):
                  raise SystemExit(
                      "Common RLIMIT_STACK: "
                      "sys/resource.h missing"
                  )

              required = (
                  "#if defined(__TERMUX__) "
                  "&& defined(__NR_getrlimit)",

                  "case __NR_getrlimit:",

                  "Arg<int> resource(0);",

                  "resource == RLIMIT_STACK",

                  ".Else("
                  "SandboxPolicyBase::"
                  "EvaluateSyscall(sysno))",
              )

              for token in required:
                  if token not in common:
                      raise SystemExit(
                          "Common RLIMIT_STACK: "
                          "expected policy token "
                          f"missing: {token}"
                      )

              count = common.count(
                  "case __NR_getrlimit:"
              )

              if count != 1:
                  raise SystemExit(
                      "Common RLIMIT_STACK: "
                      "expected exactly one "
                      "__NR_getrlimit case, "
                      f"found {count}"
                  )

              # Final source must be argument-filtered.
              case_pos = common.find(
                  "case __NR_getrlimit:"
              )

              case_end = common.find(
                  "#endif",
                  case_pos,
              )

              if (
                  case_pos < 0
                  or case_end < 0
              ):
                  raise SystemExit(
                      "Common RLIMIT_STACK: "
                      "case block boundary missing"
                  )

              case_block = common[
                  case_pos:case_end
              ]

              for token in (
                  "Arg<int> resource(0);",
                  "resource == RLIMIT_STACK",
                  "SandboxPolicyBase::"
                  "EvaluateSyscall(sysno)",
              ):
                  if token not in case_block:
                      raise SystemExit(
                          "Common RLIMIT_STACK: "
                          "restricted case token "
                          f"missing: {token}"
                      )

              broad_allow = (
                  "case __NR_getrlimit:\n"
                  "        return Allow();"
              )

              if broad_allow in common:
                  raise SystemExit(
                      "Common RLIMIT_STACK: "
                      "broad getrlimit Allow() "
                      "detected"
                  )

              print(
                  "SandboxPolicyCommon: "
                  "getrlimit(RLIMIT_STACK) "
                  "syntax sanity SUCCESS"
              )


          verify_common_getrlimit()

          print(
              "Socket/Utility PrctlPolicy "
              "injection: SUCCESS"
          )

          print(
              "Common RLIMIT_STACK "
              "injection: SUCCESS"
          )
          PY2

          # ========================================================
          # PY3: Utility process broker
          #
          # Bionic main-thread stack discovery:
          #
          #   /proc/self/stat
          #   /proc/self/maps
          #
          # Firefox rewrites them to:
          #
          #   /proc/<pid>/stat
          #   /proc/<pid>/maps
          #
          # Only Utility's own PID and only MAY_READ are added.
          # ========================================================
          \tpython3 <<'PY3'
          from pathlib import Path

          path = Path(
              "security/sandbox/linux/broker/"
              "SandboxBrokerPolicyFactory.cpp"
          )

          text = path.read_text()


          def utility_bounds(source):
              marker = (
                  "SandboxBrokerPolicyFactory::"
                  "GetUtilityProcessPolicy(int aPid) {"
              )

              start = source.find(marker)

              if start < 0:
                  raise SystemExit(
                      "GetUtilityProcessPolicy: "
                      "function not found"
                  )

              next_marker = (
                  "\n#ifndef ANDROID\n"
                  "/* static */"
              )

              end = source.find(
                  next_marker,
                  start,
              )

              if end < 0:
                  raise SystemExit(
                      "GetUtilityProcessPolicy: "
                      "end boundary not found"
                  )

              return start, end


          def inject_utility_proc_paths():
              global text

              start, end = utility_bounds(text)

              utility = text[start:end]

              anchor = (
                  '  policy->AddPath(rdonly, '
                  'nsPrintfCString("/proc/%d/exe", '
                  'aPid).get());\n'
              )

              stat_line = (
                  '  policy->AddPath(rdonly, '
                  'nsPrintfCString("/proc/%d/stat", '
                  'aPid).get());'
              )

              maps_line = (
                  '  policy->AddPath(rdonly, '
                  'nsPrintfCString("/proc/%d/maps", '
                  'aPid).get());'
              )

              has_stat = (
                  stat_line in utility
              )

              has_maps = (
                  maps_line in utility
              )

              if has_stat and has_maps:
                  print(
                      "Utility broker: "
                      "own stat/maps already present"
                  )
                  return

              if has_stat != has_maps:
                  raise SystemExit(
                      "Utility broker: partial "
                      "stat/maps policy detected"
                  )

              if utility.count(anchor) != 1:
                  raise SystemExit(
                      "Utility broker: expected "
                      "exactly one /proc/%d/exe "
                      "anchor"
                  )

              addition = (
                  anchor
                  + "#if defined(__TERMUX__)\n"
                  + stat_line
                  + "\n"
                  + maps_line
                  + "\n"
                  + "#endif\n"
              )

              utility = utility.replace(
                  anchor,
                  addition,
                  1,
              )

              text = (
                  text[:start]
                  + utility
                  + text[end:]
              )

              print(
                  "Utility broker: "
                  "own /proc/<pid>/stat and maps "
                  "injected"
              )


          def verify_utility_proc_paths():
              start, end = utility_bounds(text)

              utility = text[start:end]

              stat_line = (
                  '  policy->AddPath(rdonly, '
                  'nsPrintfCString("/proc/%d/stat", '
                  'aPid).get());'
              )

              maps_line = (
                  '  policy->AddPath(rdonly, '
                  'nsPrintfCString("/proc/%d/maps", '
                  'aPid).get());'
              )

              if utility.count(stat_line) != 1:
                  raise SystemExit(
                      "Utility broker: expected "
                      "exactly one own stat rule"
                  )

              if utility.count(maps_line) != 1:
                  raise SystemExit(
                      "Utility broker: expected "
                      "exactly one own maps rule"
                  )

              expected_block = (
                  "#if defined(__TERMUX__)\n"
                  + stat_line
                  + "\n"
                  + maps_line
                  + "\n"
                  + "#endif"
              )

              if expected_block not in utility:
                  raise SystemExit(
                      "Utility broker: stat/maps "
                      "rules are not inside the "
                      "Termux-only block"
                  )

              # No broad /proc tree/prefix permissions.
              bad_patterns = (
                  'AddTree(rdonly, "/proc',
                  'AddPrefix(rdonly, "/proc',
                  'AddTree(rdwr, "/proc',
                  'AddPrefix(rdwr, "/proc',
                  'AddTree(rdwrcr, "/proc',
                  'AddPrefix(rdwrcr, "/proc',
              )

              for bad in bad_patterns:
                  if bad in utility:
                      raise SystemExit(
                          "Utility broker: broad "
                          f"/proc policy found: {bad}"
                      )

              print(
                  "Utility broker own stat/maps "
                  "verification: SUCCESS"
              )


          inject_utility_proc_paths()
          verify_utility_proc_paths()

          path.write_text(text)

          print(
              "Utility Bionic /proc compatibility "
              "injection: SUCCESS"
          )
          PY3

          # ========================================================
          # Existing compatibility verification
          # ========================================================
          \tgrep -Fq \
          \t\t'#include <time.h>' \
          \t\tsecurity/sandbox/linux/reporter/SandboxReporterCommon.h

          \tgrep -Fq \
          \t\t'BUILDFLAG(IS_FUCHSIA) || defined(__TERMUX__)' \
          \t\tsecurity/sandbox/chromium/base/synchronization/lock_impl_posix.cc

          \tgrep -Fq \
          \t\t'RunGlibcLazyInitializers' \
          \t\tsecurity/sandbox/linux/Sandbox.cpp

          \tgrep -Fq \
          \t\t"$TERMUX_PREFIX/lib" \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \tgrep -Fq \
          \t\t"$TERMUX_PREFIX/etc" \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \tgrep -Fq \
          \t\t"$TERMUX_PREFIX/share" \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \tgrep -Fq \
          \t\t'#if !defined(ANDROID) || defined(__TERMUX__)' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'#if defined(ANDROID) && !defined(__TERMUX__)' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'PR_PAC_RESET_KEYS' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'PR_GET_DUMPABLE' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'MREMAP_MAYMOVE | MREMAP_FIXED' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          # ========================================================
          # RLIMIT_STACK verification
          # ========================================================
          \tgrep -Fq \
          \t\t'#include <sys/resource.h>' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'case __NR_getrlimit:' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          \tgrep -Fq \
          \t\t'resource == RLIMIT_STACK' \
          \t\tsecurity/sandbox/linux/SandboxFilter.cpp

          # ========================================================
          # v2.4.1 Utility /proc verification
          # ========================================================
          \tgrep -Fq \
          \t\t'nsPrintfCString("/proc/%d/stat", aPid).get()' \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \tgrep -Fq \
          \t\t'nsPrintfCString("/proc/%d/maps", aPid).get()' \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          # ========================================================
          # SHM / fonts
          # ========================================================
          \tgrep -Fq \
          \t\t"$TERMUX_PREFIX/tmp/" \
          \t\tipc/glue/SharedMemoryPlatform_posix.cpp

          \tgrep -Fq \
          \t\t"std::string shmPath(\"$TERMUX_PREFIX/tmp\")" \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \tgrep -Fq \
          \t\t'policy->AddTree(rdonly, "/system/fonts")' \
          \t\tsecurity/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

          \techo
          \techo "Sandbox source verification: SUCCESS"

          # ========================================================
          # libandroid-glob
          # ========================================================
          \tif [[ \
          \t\t! -e "$TERMUX_PREFIX/lib/libandroid-glob.so" \
          \t\t&& \
          \t\t! -e "$TERMUX_PREFIX/lib/libandroid-glob.a" \
          \t]]; then

          \t\techo "ERROR: libandroid-glob is not installed"
          \t\texit 1
          \tfi

          \tlocal _glob_src
          \tlocal _glob_so

          \t_glob_src="$TERMUX_PKG_TMPDIR/firefox-sandbox-glob-preflight.c"
          \t_glob_so="$TERMUX_PKG_TMPDIR/libfirefox-sandbox-glob-preflight.so"

          \tcat > "$_glob_src" <<'EOF'
          #include <glob.h>

          int firefox_termux_glob_preflight(void) {
              glob_t g = {0};

              int result = glob(
                  "/__termux_firefox_sandbox_preflight_no_match__",
                  GLOB_NOSORT,
                  0,
                  &g
              );

              if (result == 0) {
                  globfree(&g);
              }

              return result;
          }
          EOF

          \t"$CC" \
          \t\t-shared \
          \t\t-fPIC \
          \t\t"$_glob_src" \
          \t\t-I"$TERMUX_PREFIX/include" \
          \t\t-L"$TERMUX_PREFIX/lib" \
          \t\t-Wl,--no-undefined \
          \t\t-landroid-glob \
          \t\t-o "$_glob_so"

          \techo
          \techo "API24 glob/globfree target link test: SUCCESS"

          \t"$READELF" \
          \t\t-d "$_glob_so" || true

          \techo
          \techo "======================================"
          \techo "Known sandbox/Bionic references"
          \techo "======================================"

          \tgrep -RInE \
          \t\t'PR_PAC_RESET_KEYS|PR_GET_DUMPABLE|__NR_getrlimit|RLIMIT_STACK|/proc/%d/stat|/proc/%d/maps|mremap|MREMAP_FIXED|getrlimit|AddSharedMemoryPaths|org\.mozilla\.ipc|system/fonts|shm_open|glob\(|globfree|pthread_mutexattr_setprotocol|pthread_atfork' \
          \t\tsecurity/sandbox/linux \
          \t\tsecurity/sandbox/chromium/base/synchronization \
          \t\tipc/glue \
          \t\tmozglue/interposers \
          \t\t2>/dev/null \
          \t\t| head -n 400 || true

          \techo
          \techo "Firefox native sandbox preflight v2.4.1: SUCCESS"

          \ttermux_setup_nodejs
          '''

          preflight = preflight.replace(
              r"\t",
              "\t",
          )

          text = text.replace(
              marker,
              preflight,
              1,
          )

          # --------------------------------------------------------
          # Package revision +1
          # --------------------------------------------------------
          revision = re.search(
              r"^TERMUX_PKG_REVISION=(\d+)\s*$",
              text,
              flags=re.MULTILINE,
          )

          if revision:
              new_revision = (
                  int(revision.group(1))
                  + 1
              )

              text = (
                  text[:revision.start()]
                  + f"TERMUX_PKG_REVISION={new_revision}"
                  + text[revision.end():]
              )

          else:
              version = re.search(
                  r"^TERMUX_PKG_VERSION=.*$",
                  text,
                  flags=re.MULTILINE,
              )

              if not version:
                  raise SystemExit(
                      "TERMUX_PKG_VERSION not found"
                  )

              text = (
                  text[:version.end()]
                  + "\nTERMUX_PKG_REVISION=1"
                  + text[version.end():]
              )

          path.write_text(text)
          PY

          echo "======================================"
          echo "Modified build.sh"
          echo "======================================"

          grep \
            '^TERMUX_PKG_VERSION=' \
            x11-packages/firefox/build.sh

          grep \
            '^TERMUX_PKG_REVISION=' \
            x11-packages/firefox/build.sh

          grep \
            '^TERMUX_PKG_DEPENDS=' \
            x11-packages/firefox/build.sh

          grep -n \
            'LDFLAGS+=' \
            x11-packages/firefox/build.sh

          grep -n \
            'preflight v2.4.1' \
            x11-packages/firefox/build.sh

          grep -nE \
            'RLIMIT_STACK|Utility Bionic|/proc/%d/(stat|maps)' \
            x11-packages/firefox/build.sh \
            | head -n 100

      # ============================================================
      # 5. Static native sandbox patch
      #
      # Same core patch as the successful v2.2/v2.3.1 line.
      # ============================================================
      - name: Add Termux native sandbox port patch v2.4.1
        shell: bash
        run: |
          set -euxo pipefail

          PATCH="x11-packages/firefox/9999-termux-native-sandbox.patch"

          cat > "$PATCH" <<'PATCH'
          --- a/security/sandbox/linux/reporter/SandboxReporterCommon.h
          +++ b/security/sandbox/linux/reporter/SandboxReporterCommon.h
          @@ -5,7 +5,8 @@
           #ifndef mozilla_SandboxReporterCommon_h
           #define mozilla_SandboxReporterCommon_h
           
          +#include <time.h>
           #include <sys/types.h>
           
           #include "mozilla/IntegerTypeTraits.h"
           
          --- a/security/sandbox/chromium/base/synchronization/lock_impl_posix.cc
          +++ b/security/sandbox/chromium/base/synchronization/lock_impl_posix.cc
          @@ -92,8 +92,8 @@
           // Lock::PriorityInheritanceAvailable still must be checked as the code may
           // compile but the underlying platform still may not correctly support priority
           // inheritance locks.
          -#if BUILDFLAG(IS_FUCHSIA)
          +#if BUILDFLAG(IS_FUCHSIA) || defined(__TERMUX__)
           #define PRIORITY_INHERITANCE_LOCKS_POSSIBLE() 0
           #else
           #define PRIORITY_INHERITANCE_LOCKS_POSSIBLE() 1
           
          --- a/security/sandbox/linux/Sandbox.cpp
          +++ b/security/sandbox/linux/Sandbox.cpp
          @@ -560,12 +560,14 @@
             }
           }
           
          +#if !defined(__TERMUX__)
           static void RunGlibcLazyInitializers() {
             // Make glibc's lazy initialization of shm_open() run before sandboxing
             int fd = shm_open("/dummy", O_RDONLY, 0);
             if (fd > 0) {
               close(fd);  // In the unlikely case we actually opened something
             }
           }
          +#endif
           
           static void SandboxLateInit() {
          @@ -587,7 +589,9 @@
               }
             }
           
          +#if !defined(__TERMUX__)
             RunGlibcLazyInitializers();
          +#endif
           
             // This will run on main thread before  it is in a signal-handler context, to
           
          --- a/security/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp
          +++ b/security/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp
          @@ -319,7 +319,11 @@
           
           static void AddSharedMemoryPaths(SandboxBroker::Policy* aPolicy, pid_t aPid) {
          +#if defined(__TERMUX__)
          +  std::string shmPath("@TERMUX_PREFIX@/tmp");
          +#else
             std::string shmPath("/dev/shm");
          +#endif
             if (ipc::shared_memory::AppendPosixShmPrefix(&shmPath, aPid)) {
               aPolicy->AddPrefix(rdwrcr, shmPath.c_str());
             }
           }
          @@ -500,6 +504,13 @@
             policy->AddTree(rdonly, "/etc");
             policy->AddTree(rdonly, "/usr/share");
             policy->AddTree(rdonly, "/usr/local/share");
          +
          +#if defined(__TERMUX__)
          +  policy->AddTree(rdonly, "@TERMUX_PREFIX@/lib");
          +  policy->AddTree(rdonly, "@TERMUX_PREFIX@/etc");
          +  policy->AddTree(rdonly, "@TERMUX_PREFIX@/share");
          +  policy->AddTree(rdonly, "/system/fonts");
          +#endif
             // Various places where fonts reside
             policy->AddTree(rdonly, "/usr/X11R6/lib/X11/fonts");
             policy->AddTree(rdonly, "/nix/store");
          @@ -516,8 +527,10 @@
             policy->AddPath(rdonly, "/usr");
             policy->AddPath(rdonly, "/nix");
           
          +#if !defined(__TERMUX__)
             AddLdconfigPaths(policy);
          +#endif
             AddLdLibraryEnvPaths(policy);
           
             if (!headless) {
           
          --- a/security/sandbox/linux/SandboxFilter.cpp
          +++ b/security/sandbox/linux/SandboxFilter.cpp
          @@ -80,6 +80,15 @@
           #ifndef PR_SET_VMA_ANON_NAME
           #  define PR_SET_VMA_ANON_NAME 0
           #endif
          +
          +#if defined(__TERMUX__) && defined(__aarch64__)
          +#  ifndef PR_PAC_RESET_KEYS
          +#    define PR_PAC_RESET_KEYS 54
          +#  endif
          +#  ifndef PR_PAC_APIAKEY
          +#    define PR_PAC_APIAKEY (1UL << 0)
          +#  endif
          +#endif
           
           // The GNU libc headers define O_LARGEFILE as 0 on x86_64, but we need the
           // actual value because it shows up in file flags.
          @@ -137,7 +146,7 @@
           #endif
           
           // To avoid visual confusion between "ifdef ANDROID" and "ifndef ANDROID":
          -#ifndef ANDROID
          +#if !defined(ANDROID) || defined(__TERMUX__)
           #  define DESKTOP
           #endif
           
          @@ -792,11 +801,18 @@
             virtual ResultExpr PrctlPolicy() const {
               Arg<int> op(0);
               Arg<int> arg2(1);
               return Switch(op)
                   .Case(PR_SET_VMA,  // Tagging of anonymous memory mappings
                         If(arg2 == PR_SET_VMA_ANON_NAME, Allow()).Else(InvalidSyscall()))
          +#if defined(__TERMUX__)
          +        .Case(PR_GET_DUMPABLE, Allow())
          +#  if defined(__aarch64__)
          +        .Case(PR_PAC_RESET_KEYS,
          +              If(arg2 == PR_PAC_APIAKEY, Allow()).Else(InvalidSyscall()))
          +#  endif
          +#endif
                   .Cases({PR_GET_SECCOMP,   // BroadcastSetThreadSandbox, etc.
                           PR_SET_NAME,      // Thread creation
                           PR_SET_DUMPABLE,  // Crash reporting
                           PR_SET_PTRACER},  // Debug-mode crash handling
                          Allow())
          @@ -1177,10 +1193,17 @@
                   return Allow();
           
                   // Similarly, mremap (bugs: 1047620, 1286119, 1860267)
                 case __NR_mremap: {
                   Arg<int> flags(3);
          +#if defined(__TERMUX__)
          +        return If(flags == 0, Allow())
          +            .ElseIf(flags == MREMAP_MAYMOVE, Allow())
          +            .ElseIf(flags == (MREMAP_MAYMOVE | MREMAP_FIXED), Allow())
          +            .Else(SandboxPolicyBase::EvaluateSyscall(sysno));
          +#else
                   return If((flags & ~MREMAP_MAYMOVE) == 0, Allow())
                       .Else(SandboxPolicyBase::EvaluateSyscall(sysno));
          +#endif
                 }
           #endif
           
          @@ -1468,7 +1491,7 @@
             Maybe<ResultExpr> EvaluateSocketCall(int aCall,
                                                  bool aHasArgs) const override {
               switch (aCall) {
          -#ifdef ANDROID
          +#if defined(ANDROID) && !defined(__TERMUX__)
                 case SYS_SOCKET:
                   return Some(Error(EACCES));
           #else  // #ifdef DESKTOP
          PATCH

          git add -N "$PATCH"

          echo "======================================"
          echo "Native sandbox port patch v2.4.1"
          echo "======================================"

          cat "$PATCH"

      # ============================================================
      # 6. Recipe-level preflight v2.4.1
      #
      # Validate generator source structurally with AST.
      # Final generated C++ is separately validated in pre_configure.
      # ============================================================
      - name: Recipe preflight
        shell: bash
        run: |
          set -euxo pipefail

          BUILD_SH="x11-packages/firefox/build.sh"
          MOZCONFIG="x11-packages/firefox/mozconfig.cfg"
          PATCH="x11-packages/firefox/9999-termux-native-sandbox.patch"

          # --------------------------------------------------------
          # mozconfig
          # --------------------------------------------------------
          grep -q \
            '^ac_add_options --enable-sandbox$' \
            "$MOZCONFIG"

          grep -q \
            '^ac_add_options --disable-forkserver$' \
            "$MOZCONFIG"

          grep -q \
            '^ac_add_options --with-ccache=sccache$' \
            "$MOZCONFIG"

          # --------------------------------------------------------
          # dependencies
          # --------------------------------------------------------
          grep \
            '^TERMUX_PKG_DEPENDS=' \
            "$BUILD_SH" \
            | grep -q \
              'libandroid-glob'

          grep -q \
            -- '-landroid-glob' \
            "$BUILD_SH"

          # --------------------------------------------------------
          # static patch
          # --------------------------------------------------------
          grep -q \
            'SandboxReporterCommon.h' \
            "$PATCH"

          grep -q \
            'SandboxBrokerPolicyFactory.cpp' \
            "$PATCH"

          grep -q \
            'SandboxFilter.cpp' \
            "$PATCH"

          grep -q \
            '@TERMUX_PREFIX@/tmp' \
            "$PATCH"

          grep -q \
            '/system/fonts' \
            "$PATCH"

          grep -q \
            'PR_PAC_RESET_KEYS' \
            "$PATCH"

          grep -q \
            'PR_GET_DUMPABLE' \
            "$PATCH"

          grep -q \
            'MREMAP_MAYMOVE | MREMAP_FIXED' \
            "$PATCH"

          grep -q \
            '#if !defined(ANDROID) || defined(__TERMUX__)' \
            "$PATCH"

          grep -q \
            '#if defined(ANDROID) && !defined(__TERMUX__)' \
            "$PATCH"

          # --------------------------------------------------------
          # build.sh shell syntax + heredoc boundary validation
          # --------------------------------------------------------
          bash -n "$BUILD_SH"

          echo \
            "Generated build.sh shell syntax: SUCCESS"

          # --------------------------------------------------------
          # Extract and AST-parse PY2 / PY3.
          # --------------------------------------------------------
          python3 - "$BUILD_SH" <<'PY'
          from pathlib import Path
          import ast
          import re
          import sys
          import textwrap

          path = Path(sys.argv[1])
          text = path.read_text()


          def extract_python_heredoc(label):
              pattern = re.compile(
                  rf"(?ms)"
                  rf"^[ \t]*python3 <<'{label}'[ \t]*\n"
                  rf"(.*?)"
                  rf"^[ \t]*{label}[ \t]*$"
              )

              matches = pattern.findall(text)

              if len(matches) != 1:
                  raise SystemExit(
                      f"ERROR: expected exactly one "
                      f"{label} heredoc, "
                      f"found {len(matches)}"
                  )

              return textwrap.dedent(
                  matches[0]
              )


          def parse_python(label):
              source = extract_python_heredoc(
                  label
              )

              try:
                  tree = ast.parse(
                      source,
                      filename=f"<{label}>",
                      mode="exec",
                  )
              except SyntaxError:
                  print(
                      f"ERROR: embedded {label} "
                      "has invalid Python syntax"
                  )
                  raise

              print(
                  f"Embedded {label} Python "
                  "syntax: SUCCESS"
              )

              return source, tree


          def functions_by_name(tree):
              return {
                  node.name: node
                  for node in ast.walk(tree)
                  if isinstance(
                      node,
                      (
                          ast.FunctionDef,
                          ast.AsyncFunctionDef,
                      ),
                  )
              }


          def function_source(
              source,
              functions,
              name,
          ):
              if name not in functions:
                  raise SystemExit(
                      f"ERROR: function missing: "
                      f"{name}"
                  )

              result = ast.get_source_segment(
                  source,
                  functions[name],
              )

              if not result:
                  raise SystemExit(
                      "ERROR: could not extract "
                      f"function source: {name}"
                  )

              return result


          # ========================================================
          # PY2 validation
          # ========================================================
          py2, tree2 = parse_python("PY2")
          funcs2 = functions_by_name(tree2)

          for name in (
              "class_bounds",
              "prctl_bounds",
              "inject_prctl_rules",
              "inject_common_getrlimit_stack",
              "verify_prctl",
              "verify_common_getrlimit",
          ):
              if name not in funcs2:
                  raise SystemExit(
                      "ERROR: required PY2 "
                      f"function missing: {name}"
                  )

          pac = function_source(
              py2,
              funcs2,
              "inject_prctl_rules",
          )

          for token in (
              ".Case(PR_GET_DUMPABLE, ",
              ".Case(PR_PAC_RESET_KEYS,",
              "PR_PAC_APIAKEY",
              "InvalidSyscall()",
          ):
              if token not in pac:
                  raise SystemExit(
                      "ERROR: PAC injector "
                      f"token missing: {token!r}"
                  )

          for bad in (
              ".CASES((PR_GET_DUMPABLE",
              ".CASES((PR_PAC_RESET_KEYS",
          ):
              if bad in pac:
                  raise SystemExit(
                      "ERROR: stale invalid "
                      f"PAC API: {bad}"
                  )

          verify_pac = function_source(
              py2,
              funcs2,
              "verify_prctl",
          )

          for token in (
              "PR_GET_DUMPABLE",
              "PR_PAC_RESET_KEYS",
              "PR_PAC_APIAKEY",
              "expected rule missing",
          ):
              if token not in verify_pac:
                  raise SystemExit(
                      "ERROR: PAC verifier "
                      f"token missing: {token!r}"
                  )

          rlimit = function_source(
              py2,
              funcs2,
              "inject_common_getrlimit_stack",
          )

          for token in (
              "SandboxPolicyCommon",
              "ContentSandboxPolicy",
              "case __NR_getrlimit:",
              "Arg<int> resource(0);",
              "resource == RLIMIT_STACK",
              "SandboxPolicyBase::",
              "EvaluateSyscall(sysno)",
          ):
              if token not in rlimit:
                  raise SystemExit(
                      "ERROR: RLIMIT_STACK "
                      f"injector token missing: "
                      f"{token!r}"
                  )

          verify_rlimit = function_source(
              py2,
              funcs2,
              "verify_common_getrlimit",
          )

          for token in (
              "case __NR_getrlimit:",
              "Arg<int> resource(0);",
              "resource == RLIMIT_STACK",
              "SandboxPolicyBase::",
              "EvaluateSyscall(sysno)",
              "broad getrlimit Allow()",
          ):
              if token not in verify_rlimit:
                  raise SystemExit(
                      "ERROR: RLIMIT_STACK "
                      f"verifier token missing: "
                      f"{token!r}"
                  )

          calls2 = []

          for node in ast.walk(tree2):
              if (
                  isinstance(node, ast.Call)
                  and isinstance(
                      node.func,
                      ast.Name,
                  )
              ):
                  calls2.append(
                      node.func.id
                  )

          if calls2.count(
              "inject_prctl_rules"
          ) < 2:
              raise SystemExit(
                  "ERROR: expected two "
                  "inject_prctl_rules calls"
              )

          if calls2.count(
              "inject_common_getrlimit_stack"
          ) < 1:
              raise SystemExit(
                  "ERROR: common RLIMIT_STACK "
                  "injector is not called"
              )

          if calls2.count(
              "verify_prctl"
          ) < 2:
              raise SystemExit(
                  "ERROR: expected two "
                  "verify_prctl calls"
              )

          if calls2.count(
              "verify_common_getrlimit"
          ) < 1:
              raise SystemExit(
                  "ERROR: RLIMIT_STACK verifier "
                  "is not called"
              )

          print(
              "PY2 injector validation: SUCCESS"
          )

          # ========================================================
          # PY3 validation
          # ========================================================
          py3, tree3 = parse_python("PY3")
          funcs3 = functions_by_name(tree3)

          for name in (
              "utility_bounds",
              "inject_utility_proc_paths",
              "verify_utility_proc_paths",
          ):
              if name not in funcs3:
                  raise SystemExit(
                      "ERROR: required PY3 "
                      f"function missing: {name}"
                  )

          # --------------------------------------------------------
          # Correct location for GetUtilityProcessPolicy validation.
          # --------------------------------------------------------
          utility_locator = function_source(
              py3,
              funcs3,
              "utility_bounds",
          )

          for token in (
              "GetUtilityProcessPolicy",
              "SandboxBrokerPolicyFactory::",
              "function not found",
              "end boundary not found",
          ):
              if token not in utility_locator:
                  raise SystemExit(
                      "ERROR: Utility locator "
                      f"token missing: {token!r}"
                  )

          print(
              "PY3 Utility function locator "
              "validation: SUCCESS"
          )

          # --------------------------------------------------------
          # Injector body
          # --------------------------------------------------------
          inject_proc = function_source(
              py3,
              funcs3,
              "inject_utility_proc_paths",
          )

          for token in (
              "utility_bounds",
              "/proc/%d/exe",
              "/proc/%d/stat",
              "/proc/%d/maps",
              "#if defined(__TERMUX__)",
              "rdonly",
          ):
              if token not in inject_proc:
                  raise SystemExit(
                      "ERROR: Utility /proc "
                      f"injector token missing: "
                      f"{token!r}"
                  )

          print(
              "PY3 Utility /proc injector "
              "body validation: SUCCESS"
          )

          # --------------------------------------------------------
          # Final-source verifier
          # --------------------------------------------------------
          verify_proc = function_source(
              py3,
              funcs3,
              "verify_utility_proc_paths",
          )

          # Validate structural policy checks rather than
          # reconstructed runtime error-message strings.
          for token in (
              "/proc/%d/stat",
              "/proc/%d/maps",
              "expected_block",
              "bad_patterns",
              "AddTree(rdonly,",
              "AddPrefix(rdonly,",
              "AddTree(rdwr,",
              "AddPrefix(rdwr,",
              "AddTree(rdwrcr,",
              "AddPrefix(rdwrcr,",
          ):
              if token not in verify_proc:
                  raise SystemExit(
                      "ERROR: Utility /proc "
                      f"verifier token missing: "
                      f"{token!r}"
                  )

          print(
              "PY3 Utility /proc result "
              "verifier validation: SUCCESS"
          )

          print(
              "PY3 Utility /proc result "
              "verifier validation: SUCCESS"
          )

          # --------------------------------------------------------
          # PY3 call graph
          # --------------------------------------------------------
          calls3 = []

          for node in ast.walk(tree3):
              if (
                  isinstance(node, ast.Call)
                  and isinstance(
                      node.func,
                      ast.Name,
                  )
              ):
                  calls3.append(
                      node.func.id
                  )

          if calls3.count(
              "utility_bounds"
          ) < 2:
              raise SystemExit(
                  "ERROR: Utility locator is "
                  "not used by injector/verifier"
              )

          if calls3.count(
              "inject_utility_proc_paths"
          ) < 1:
              raise SystemExit(
                  "ERROR: Utility /proc "
                  "injector is not called"
              )

          if calls3.count(
              "verify_utility_proc_paths"
          ) < 1:
              raise SystemExit(
                  "ERROR: Utility /proc "
                  "verifier is not called"
              )

          print(
              "PY3 Utility /proc injector "
              "validation: SUCCESS"
          )

          print(
              "Dynamic injector source "
              "validation: SUCCESS"
          )
          PY

          # --------------------------------------------------------
          # Directly modified recipe/config syntax
          # --------------------------------------------------------
          git diff --check -- \
            x11-packages/firefox/build.sh \
            x11-packages/firefox/mozconfig.cfg

          echo
          echo "Recipe preflight: SUCCESS"

      # ============================================================
      # 7. Runtime helper
      # ============================================================
      - name: Create runtime test helper
        shell: bash
        run: |
          set -euxo pipefail

          cat > termux-firefox-native-sandbox-test.sh <<'SCRIPT'
          #!/data/data/com.termux/files/usr/bin/bash

          set -u

          LEVEL="${FIREFOX_SANDBOX_LEVEL:-3}"
          PROFILE="${FIREFOX_SANDBOX_PROFILE:-$HOME/.mozilla/firefox-termux-sandbox-bringup}"
          LOG="${FIREFOX_SANDBOX_LOG:-$HOME/firefox-native-sandbox-runtime.log}"
          DISABLE_AUX="${FIREFOX_SANDBOX_DISABLE_AUX:-0}"

          mkdir -p "$PROFILE"

          cat > "$PROFILE/user.js" <<EOF
          user_pref("security.sandbox.content.level", ${LEVEL});
          user_pref("browser.shell.checkDefaultBrowser", false);
          user_pref("browser.startup.homepage", "about:blank");
          EOF

          echo "======================================"
          echo "Termux Firefox native sandbox v2.4.1"
          echo "======================================"
          echo "level   : $LEVEL"
          echo "profile : $PROFILE"
          echo "log     : $LOG"
          echo "aux off : $DISABLE_AUX"
          echo

          uname -a || true

          getprop \
            ro.build.version.release \
            2>/dev/null || true

          getprop \
            ro.build.version.sdk \
            2>/dev/null || true

          if [ "$DISABLE_AUX" = "1" ]; then

            export \
              MOZ_DISABLE_SOCKET_PROCESS_SANDBOX=1

            export \
              MOZ_DISABLE_UTILITY_SANDBOX=1

            export \
              MOZ_DISABLE_RDD_SANDBOX=1

          else

            unset \
              MOZ_DISABLE_SOCKET_PROCESS_SANDBOX \
              2>/dev/null || true

            unset \
              MOZ_DISABLE_UTILITY_SANDBOX \
              2>/dev/null || true

            unset \
              MOZ_DISABLE_RDD_SANDBOX \
              2>/dev/null || true
          fi

          set +e

          MOZ_SANDBOX_LOGGING=1 \
            firefox \
              --no-remote \
              --profile "$PROFILE" \
              about:blank \
              2>&1 \
            | tee "$LOG"

          rc=${PIPESTATUS[0]}

          set -e

          echo
          echo "======================================"
          echo "Sandbox-focused runtime log"
          echo "======================================"

          grep -nEi \
            'sandbox|seccomp|sigsys|violation|syscall|163|198|getrlimit|rlimit_stack|socket|/proc/self/(stat|maps)|/proc/[0-9]+/(stat|maps)|logdw|prctl|mremap|linker_cfi|shadowwrite|map_failed|org\.mozilla\.ipc|shmsegmentswriter|system/fonts|timezone|tzdata|__properties__|channel error' \
            "$LOG" \
            | tail -n 900 || true

          echo
          echo "Firefox exit code: $rc"

          exit "$rc"
          SCRIPT

          chmod +x \
            termux-firefox-native-sandbox-test.sh

          bash -n \
            termux-firefox-native-sandbox-test.sh

          grep -Fq \
            'PROFILE="${FIREFOX_SANDBOX_PROFILE:-$HOME/.mozilla/firefox-termux-sandbox-bringup}"' \
            termux-firefox-native-sandbox-test.sh

          echo \
            "Runtime helper syntax: SUCCESS"

      # ============================================================
      # 8. Save custom changes
      # ============================================================
      - name: Save custom changes
        shell: bash
        run: |
          set -euxo pipefail

          git diff -- \
            x11-packages/firefox/ \
            | tee \
              firefox-native-sandbox-changes.diff

          git status --short \
            x11-packages/firefox/

      # ============================================================
      # 9. Restore compiler cache
      # ============================================================
      - name: Restore Firefox sccache
        id: sccache-restore
        uses: actions/cache/restore@v4
        with:
          path: .sccache
          key: firefox-native-sandbox-sccache-aarch64-v14-${{ github.run_id }}
          restore-keys: |
            firefox-native-sandbox-sccache-aarch64-v14-
            firefox-native-sandbox-sccache-aarch64-v13-
            firefox-native-sandbox-sccache-aarch64-v12-
            firefox-native-sandbox-sccache-aarch64-v11-
            firefox-native-sandbox-sccache-aarch64-v10-
            firefox-native-sandbox-sccache-aarch64-v9-
            firefox-native-sandbox-sccache-aarch64-v8-
            firefox-native-sandbox-sccache-aarch64-v7-

      # ============================================================
      # 10. Prepare cache
      # ============================================================
      - name: Prepare sccache directory
        shell: bash
        run: |
          set -euxo pipefail

          mkdir -p .sccache

          sudo chown -R \
            "$(id -u):$(id -g)" \
            .sccache

          chmod -R \
            a+rwX \
            .sccache

          ls -ld .sccache

          du -sh \
            .sccache || true

      # ============================================================
      # 11. zram
      # ============================================================
      - name: Enable zram
        uses: ./.github/actions/zram
        with:
          algorithm: zstd
          size: 16G
          priority: 100
          device_name: /dev/zram0

      # ============================================================
      # 12. Official Termux builder
      # ============================================================
      - name: Prepare official Termux builder
        shell: bash
        run: |
          set -euo pipefail

          BASE_IMAGE="ghcr.io/termux/package-builder:latest"
          PULLED=0

          for attempt in 1 2 3 4 5; do

            echo
            echo "======================================"
            echo "Base image pull ${attempt}/5"
            echo "======================================"

            if docker pull "$BASE_IMAGE"; then
              PULLED=1
              break
            fi

            if [ "$attempt" -lt 5 ]; then
              delay=$((attempt * 15))

              echo \
                "Pull failed. Retry in ${delay}s..."

              sleep "$delay"
            fi
          done

          if [ "$PULLED" -ne 1 ]; then

            docker build \
              -t "$BASE_IMAGE" \
              scripts/

            docker buildx prune \
              -af || true
          fi

          docker image inspect \
            "$BASE_IMAGE" \
            --format \
            'ID={{.Id}} Size={{.Size}} Created={{.Created}}'

      # ============================================================
      # 13. Builder + sccache
      # ============================================================
      - name: Build Firefox builder with sccache
        shell: bash
        run: |
          set -euxo pipefail

          cat > /tmp/Dockerfile.firefox-sccache <<'DOCKERFILE'
          FROM ghcr.io/termux/package-builder:latest

          USER root

          RUN apt-get update \
              && apt-get install \
                   -yq \
                   --no-install-recommends \
                   sccache \
              && apt-get clean \
              && rm -rf /var/lib/apt/lists/*

          USER builder:builder

          WORKDIR /home/builder/termux-packages
          DOCKERFILE

          docker build \
            -f /tmp/Dockerfile.firefox-sccache \
            -t "$TERMUX_BUILDER_IMAGE_NAME" \
            .

          docker image inspect \
            "$TERMUX_BUILDER_IMAGE_NAME" \
            --format \
            'ID={{.Id}} Size={{.Size}} Created={{.Created}}'

      # ============================================================
      # 14. Start builder
      # ============================================================
      - name: Start Termux builder
        shell: bash
        run: |
          set -euxo pipefail

          docker rm -f \
            termux-package-builder \
            2>/dev/null || true

          export \
            TERMUX_DOCKER_RUN_EXTRA_ARGS="--volume ${GITHUB_WORKSPACE}/.sccache:/home/builder/.cache/sccache"

          ./scripts/run-docker.sh true

          docker ps -a

          docker inspect \
            termux-package-builder \
            --format \
            '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'

      # ============================================================
      # 15. Verify sccache
      # ============================================================
      - name: Verify sccache
        shell: bash
        run: |
          set -euxo pipefail

          ./scripts/run-docker.sh \
            bash -lc '
              set -euxo pipefail

              export SCCACHE_DIR=/home/builder/.cache/sccache
              export SCCACHE_CACHE_SIZE=6G

              id

              command -v sccache
              sccache --version

              ls -ld "$SCCACHE_DIR"

              echo test \
                > "$SCCACHE_DIR/.write-test"

              cat \
                "$SCCACHE_DIR/.write-test"

              rm \
                "$SCCACHE_DIR/.write-test"
            '

      # ============================================================
      # 16. Ubuntu runner cleanup workaround
      # ============================================================
      - name: Patch Termux free-space script
        shell: bash
        run: |
          set -euxo pipefail

          SCRIPT="scripts/free-space.sh"

          sed -i \
            '/ruby3\.2-doc/d' \
            "$SCRIPT"

          if grep -q \
            'ruby3.2-doc' \
            "$SCRIPT"; then

            echo \
              "ERROR: ruby3.2-doc still present"

            exit 1
          fi

      # ============================================================
      # 17. Free runner disk space
      # ============================================================
      - name: Free runner disk space
        shell: bash
        run: |
          set -euxo pipefail

          echo "======================================"
          echo "Disk before cleanup"
          echo "======================================"

          df -h

          ./scripts/free-space.sh

          echo
          echo "======================================"
          echo "Disk after cleanup"
          echo "======================================"

          df -h

      # ============================================================
      # 18. Reset cache statistics
      # ============================================================
      - name: Reset sccache statistics
        shell: bash
        run: |
          set -euxo pipefail

          ./scripts/run-docker.sh \
            env \
              SCCACHE_DIR=/home/builder/.cache/sccache \
              SCCACHE_CACHE_SIZE=6G \
            sccache \
              --zero-stats

      # ============================================================
      # 19. Build Firefox
      # ============================================================
      - name: Build Firefox
        id: build-firefox
        shell: bash
        run: |
          set -o pipefail

          echo "======================================"
          echo "Starting Firefox native sandbox v2.4.1"
          echo "======================================"

          ./scripts/run-docker.sh \
            env \
              SCCACHE_DIR=/home/builder/.cache/sccache \
              SCCACHE_CACHE_SIZE=6G \
            ./build-package.sh \
              -f \
              -I \
              -a aarch64 \
              firefox \
            2>&1 \
            | tee firefox-build.log

      # ============================================================
      # 20. Cache statistics
      # ============================================================
      - name: Show sccache statistics
        if: always()
        shell: bash
        run: |
          ./scripts/run-docker.sh \
            env \
              SCCACHE_DIR=/home/builder/.cache/sccache \
              SCCACHE_CACHE_SIZE=6G \
            sccache \
              --show-stats \
            || true

      # ============================================================
      # 21. Diagnostics
      # ============================================================
      - name: Build diagnostics
        if: always()
        shell: bash
        run: |
          echo "======================================"
          echo "Firefox modifications"
          echo "======================================"

          git diff -- \
            x11-packages/firefox/ \
            || true

          echo
          echo "======================================"
          echo "Patch reject files"
          echo "======================================"

          ./scripts/run-docker.sh \
            bash -lc '
              find \
                /home/builder/.termux-build/firefox \
                -type f \
                -name "*.rej" \
                -print \
                -exec sh -c '"'"'
                  echo
                  echo "---------- $1 ----------"
                  cat "$1"
                '"'"' _ {} \;
            ' \
            2>/dev/null || true

          echo
          echo "======================================"
          echo "Final SandboxFilter compatibility rules"
          echo "======================================"

          ./scripts/run-docker.sh \
            bash -lc '
              SRC=/home/builder/.termux-build/firefox/src/security/sandbox/linux/SandboxFilter.cpp

              if [ -f "$SRC" ]; then
                grep -nE \
                  "PR_PAC_RESET_KEYS|PR_GET_DUMPABLE|__NR_getrlimit|RLIMIT_STACK|MREMAP_FIXED|sys/resource.h" \
                  "$SRC" \
                  || true
              fi
            ' \
            2>/dev/null || true

          echo
          echo "======================================"
          echo "Final Utility broker compatibility rules"
          echo "======================================"

          ./scripts/run-docker.sh \
            bash -lc '
              SRC=/home/builder/.termux-build/firefox/src/security/sandbox/linux/broker/SandboxBrokerPolicyFactory.cpp

              if [ -f "$SRC" ]; then
                grep -nE \
                  "GetUtilityProcessPolicy|/proc/%d/(exe|stat|maps|statm|smaps)" \
                  "$SRC" \
                  || true
              fi
            ' \
            2>/dev/null || true

          echo
          echo "======================================"
          echo "Disk / Docker / cache"
          echo "======================================"

          df -h || true
          docker ps -a || true

          du -sh \
            .sccache \
            2>/dev/null || true

          echo
          echo "======================================"
          echo "Sandbox / Bionic markers"
          echo "======================================"

          grep -nEi \
            'preflight|Utility broker|Bionic /proc|/proc/%d/stat|/proc/%d/maps|RLIMIT_STACK|getrlimit|__NR_getrlimit|syntax sanity|SocketProcessSandboxPolicy|UtilitySandboxPolicy|PrctlPolicy injection|security/sandbox|libmozsandbox|libxul|PR_PAC_RESET_KEYS|PR_GET_DUMPABLE|mremap|MREMAP_FIXED|AddSharedMemoryPaths|org\.mozilla\.ipc|system/fonts|shm_open|ShmSegmentsWriter|undefined symbol|duplicate symbol|ld\.lld: error|patch.*FAILED|Hunk.*FAILED|reject' \
            firefox-build.log \
            2>/dev/null \
            | tail -n 900 || true

          echo
          echo "======================================"
          echo "Generated DEB files"
          echo "======================================"

          find output \
            -type f \
            -name 'firefox_*.deb' \
            -print \
            2>/dev/null || true

          echo
          echo "======================================"
          echo "Last 900 build lines"
          echo "======================================"

          tail -n 900 \
            firefox-build.log \
            2>/dev/null || true

      # ============================================================
      # 22. Save compiler cache
      # ============================================================
      - name: Save Firefox sccache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .sccache
          key: firefox-native-sandbox-sccache-aarch64-v14-${{ github.run_id }}

      # ============================================================
      # 23. Collect package
      # ============================================================
      - name: Collect Firefox package
        if: success()
        shell: bash
        run: |
          set -euxo pipefail

          mkdir -p artifacts

          find output \
            -type f \
            -name 'firefox_*.deb' \
            -print \
            -exec cp -v '{}' artifacts/ ';'

          cp \
            firefox-native-sandbox-changes.diff \
            artifacts/

          cp \
            x11-packages/firefox/9999-termux-native-sandbox.patch \
            artifacts/

          cp \
            termux-firefox-native-sandbox-test.sh \
            artifacts/

          DEB="$(
            find artifacts \
              -maxdepth 1 \
              -type f \
              -name 'firefox_*.deb' \
              -print -quit
          )"

          if [ -z "$DEB" ]; then
            echo "ERROR: Firefox .deb not found"
            exit 1
          fi

          (
            cd artifacts

            sha256sum \
              firefox_*.deb \
              > SHA256SUMS
          )

          echo
          echo "======================================"
          echo "Final artifacts"
          echo "======================================"

          ls -lh artifacts/

      # ============================================================
      # 24. Upload package
      # ============================================================
      - name: Upload Firefox native sandbox package
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: firefox-native-sandbox-aarch64-v2.4.1
          path: artifacts/
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

      # ============================================================
      # 25. Upload diagnostics
      # ============================================================
      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: firefox-native-sandbox-build-diagnostics-v2.4.1
          path: |
            firefox-build.log
            firefox-native-sandbox-changes.diff
            x11-packages/firefox/9999-termux-native-sandbox.patch
            termux-firefox-native-sandbox-test.sh
          if-no-files-found: ignore
          retention-days: 14
